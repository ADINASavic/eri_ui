# ERI: 아웃게임 기획 v1.0

2026-07-22 · 게임명 **ERI: (Emotive Rhythm Interaction)** · 목업: `outgame_mockup.html`

방향성 한 줄: Malody류 시뮬레이터의 기능성 위에 **신뢰 가능한 점수 생태계**(OFFICIAL/RANKED/UNRANKED 차트 등급, 검증된 플레이만 레이팅 합산)를 얹는다. 비주얼은 타이틀만 다크 네온(로고 톤), 나머지 화면은 크림 페이퍼 + 차콜 대비. UI에 ms는 절대 노출하지 않고 모든 수치는 정수/0.5 스텝.

## 부팅 · 타이틀

FMOD 로고 전체화면(자동 진행, 키 스킵) → ERI 풀 로고 + PRESS ANY KEY → 아무 키 입력 시 로고 텍스트가 사라지고 캐릭터만 우측에 남으며 좌측에서 PLAY / SETTINGS / IMPORT / QUIT 메뉴가 슬라이드 인. 우상단에 프로필(닉네임·레이팅·온라인 상태)이 함께 나타난다.

## 선곡

정사각 SongCard 가로 캐러셀(재킷 + 하단 오버레이에 제목·아티스트·PB등급, 좌상단 난이도 컬러 블록, 우상단 신뢰 뱃지). 상단 중앙에 폴더 바 3칸 — [이전] [현재(강조+곡 수)] [다음] — 이 포커스를 따라다니며, 클릭으로만 폴더 점프. 폴더는 정렬 기준의 그룹(난이도→Lv 상수, 아티스트/제목→이니셜, 업로드→월별 등)이고 리스트 자체는 끊김 없이 쭉 이어진다. 난이도 정렬일 때만 카드가 차트 단위(같은 곡의 Hard/Master가 각자 Lv 폴더에)가 되어 난이도 선택이 사라진다. 그 외 정렬에선 카드에 표시할 난이도를 글로벌 커서(마지막 본 난이도 이름)가 결정. 상단바에 검색(제목/아티스트/charter, 검색 중 폴더 해제)·정렬 드롭다운·필터(등급/클리어/charter), 하단에 키 안내와 프로필 패널(프사·레이팅·레벨·설정 버튼).

## Decide (선곡 확정 ↔ 인게임 사이)

좌측에 큰 재킷 + 곡 정보 + 신뢰 뱃지 + 난이도 전체를 선곡창과 같은 컬러 칩 리스트로 표시(클릭 또는 1/3로 변경). 우측에 리더보드 탭 — 글로벌/친구/내 기록. UNRANKED 차트는 글로벌 탭이 비활성화되고 로컬 기록만 보인다. 하단에 배속 스테퍼(0.5 단위, 설정과 공유되는 전역 값)와 Press [5] To Start. 플레이하지 않고 리더보드만 보고 Back으로 돌아가는 동선을 허용한다.

## 설정

카테고리 하나가 카드 하나를 통째로 차지하는 캐러셀(4/6 또는 좌우 화살표, 하단 도트). 카드 구성: **OFFSET**(Input Timing / Music Sync 스텝 스테퍼 + 캘리브레이션 마법사), **PLAY**(배속, Grow Time, Appear Lead, Perfect SFX 볼륨, 마우스 감도 + 사용 토글), **VIEW**(전체화면), **PLAYER**(닉네임 — Save 시 재시작, 레이팅·로그인 상태). 각 카드에 Save/Reset과 피드백 텍스트.

## 키맵

4/6 카드·설정탭 이동, 1/3 난이도(선곡=커서, Decide=확정), 5 결정/시작, 9 설정, Esc 뒤로. 2/7/8은 여유 키. 폴더 이동과 정렬 변경은 마우스 전용.

## 레이팅 (온라인)

차트 레이팅 = 레벨 × 정확도, 프로필 레이팅 = top 30 합산. official·ranked 차트의 무변조 플레이만 반영, 배속은 표기만 되고 레이팅에 영향 없음. 오프라인일 땐 모든 화면이 로컬 기록만으로 동작.
